<div class="jumbotron text-center">
	<h1 class="display-3">WELCOME</h1>
	{{#if user}}
		<h2 class="display-2">{{firstname user.name}}</h2>
		<p class="lead">Jot down your ideas for your next youtube videos</p>
		<a href="/ideas/add" class="btn btn-dark btn-lg">Add Video</a>
	{{else}}
		<h4 class=""><bold>You aren't Logged in</bold></h4>
	{{/if}}
	
</div>
